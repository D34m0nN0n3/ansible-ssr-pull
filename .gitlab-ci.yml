  stages:
    - texdocs

  .check_rule:
    rules:
      - exists:
        - material

  check:
    tags:
      - mkdocs
    stage: texdocs
    script:
      - echo exist
    rules:
      - exists:
        - material

  build:
    only:
      - main
#    refs:
#      - master
#      - merge_requests
    tags:
      - mkdocs
    stage: texdocs
    script:
      - echo $CI_PROJECT_NAME
      - pwd
      - ls -alZ
      - hostname -f
      - whoami & id
